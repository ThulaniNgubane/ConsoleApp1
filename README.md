ConsoleApp1 folder on top is the that contains all the voice, bin, obj and properties folders. I also have all the classes that are in the project: consloeApp1.csproj, consloeApp1.csproj.Backup.tmp ,logo.sc, program.cs,  chatbot.sc, dockerfile and ConsoleApp1.csproj.user project file
Voice folder contains the WAV file of the sound in the project
.dockerignore file
Git Attributes Source file called .gitattributes
Git Ignore Source file called .gitignore
ConsoleApp1.sin is the Visual Studio project file that has all the codes to play sound, play sound and as a responsive chatbot.
